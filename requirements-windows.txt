# Windows-friendly requirements
# Try installing these in order to avoid compilation issues

# Core dependencies (usually have pre-built wheels)
requests>=2.25.0
pandas>=1.3.0
numpy>=1.21.0

# Locust and dependencies - install separately if geventhttpclient fails
# Option 1: Try with pre-built wheels
# pip install --only-binary :all: locust

# Option 2: Install build dependencies first
# pip install --upgrade pip setuptools wheel
# pip install locust

# Option 3: Use Conda
# conda install -c conda-forge locust

# Option 4: Use Docker (see loadgen/locust_docker.py)

